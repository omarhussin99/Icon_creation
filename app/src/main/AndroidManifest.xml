<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realness.iconcreation">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/colored_logo"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@drawable/colored_logo"
        android:supportsRtl="false"
        android:theme="@style/Theme.IconCreation">
        <activity android:name=".ui.view.HomeActivity" />
        <activity android:name=".ui.view.LoginActivity" />
        <activity android:name=".ui.view.SplashActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>